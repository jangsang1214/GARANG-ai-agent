{
  "name": "garang-ai-agent",
  "version": "0.2.0",
  "private": true,
  "description": "GARANG Founder OS and autonomous coding-agent core",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/permissions.test.js dist/tests/planner.test.js dist/tests/recovery.test.js dist/tests/project-state.test.js dist/tests/filesystem.test.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
