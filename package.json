{
  "name": "garang-ai-agent",
  "version": "0.1.0",
  "private": true,
  "description": "GARANG autonomous coding agent core",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}